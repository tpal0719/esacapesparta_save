plugins {
    id 'java'
}

bootJar {
    enabled = true
    archiveFileName = 'escape-consumer.jar' // 생성될 JAR 파일 이름 설정
}
jar{enabled = false}

dependencies {
    implementation project(':escape-core')
}
